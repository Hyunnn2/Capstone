import React, { useRef, useEffect } from 'react';
import DragIndicatorIcon from '@mui/icons-material/DragIndicator';

const ResizableButton = () => {

    return (
        <div className='ResizableButton'>
            <DragIndicatorIcon/>
        </div>
    );
};

export default ResizableButton;
